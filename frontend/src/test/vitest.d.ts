import '@testing-library/jest-dom';
import { beforeAll, beforeEach, afterAll, afterEach, describe, expect, it, test, vi } from 'vitest';

beforeAll(() => {});
beforeEach(() => {});
afterAll(() => {});
afterEach(() => {});
describe('global', () => {});
expect(true).toBe(true);
it('global', () => {});
test('global', () => {});
vi.fn();
